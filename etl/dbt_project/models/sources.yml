
version: 2

sources:
  - name: etl_data
    schema: main
    tables:
      - name: raw_orders
        description: "Raw orders data loaded from CSV"
        meta:
          external_location: "../data/raw_orders.csv"
